name: Update gist
on:
  repository_dispatch:
    types: [build-event]
  schedule:
    - cron: "0 0 * * *"
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:
jobs:
  update-gist:
    runs-on: ubuntu-latest
    steps:
      - name: Update gist
        uses: maxam2017/productive-box@master
        env:
          GH_TOKEN: ${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}
          GIST_ID: a4de41a8fa4743f69b269631ab5c9370
          TIMEZONE: Asia/Seoul
